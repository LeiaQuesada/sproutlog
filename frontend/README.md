# To run the frontend

Ensure you are in the root of the frontend directory,
Then run the following commands in the terminal:

# Run the web server:

npm install
npm run dev

## In your browser, go to

http://localhost:5173/
